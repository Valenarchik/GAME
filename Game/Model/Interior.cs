using System.Drawing;

namespace Game.Model
{
    public class Interior : GameObject
    {
        public Interior(GameLevel level, Point position, Size size)
            : base(level, position, size)
        {
        }
    }
}